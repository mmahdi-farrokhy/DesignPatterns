package observer.order;

import observer.contracts.Observer;
import observer.contracts.Subject;

public class OrderProcessingObserver implements Observer {
    @Override
    public void update(Subject subject) {

    }
}
